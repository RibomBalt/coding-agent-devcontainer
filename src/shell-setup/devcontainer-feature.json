{
    "id": "shell-setup",
    "version": "1.0.0",
    "name": "Shell Setup (zsh + powerlevel10k)",
    "description": "Installs the powerlevel10k zsh theme via zsh-in-docker and configures fzf shell integration and persistent history.",
    "options": {
        "zsh_in_docker_version": {
            "type": "string",
            "default": "1.2.0",
            "description": "Version of deluan/zsh-in-docker to use"
        }
    },
    "installsAfter": [
        "system-tools"
    ]
}
