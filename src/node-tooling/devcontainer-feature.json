{
    "id": "node-tooling",
    "version": "1.0.0",
    "name": "Node.js Tooling (pnpm + Playwright)",
    "description": "Installs pnpm globally with the configured npm registry, sets up the pnpm global directory, and installs Playwright Chromium browsers.",
    "options": {
        "npm_mirror": {
            "type": "string",
            "default": "https://registry.npmmirror.com",
            "description": "NPM registry mirror to use"
        },
        "playwright_version": {
            "type": "string",
            "default": "1.59.1",
            "description": "Playwright version used to install Chromium browsers"
        }
    },
    "installsAfter": [
        "system-tools"
    ]
}
